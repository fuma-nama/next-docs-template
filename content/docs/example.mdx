---
title: Example
---

something that is funny.

## Heading 2

1. Create a file
2. Create a folder
3. Delete everything

> I don't know what is going on - Money

```ts
console.log("Hello World");
```

### Heading 3

In fact, most of the React developers spend time on finding a library instead of writing their own implementation.

#### Heading 4

Code is pretty, due to `rehype-pretty-code`.

```tsx
return <MyCoolComponent>Hello World</MyCoolComponent>;
```

| Name             |                                          |
| ---------------- | ---------------------------------------- |
| `next-docs-zeta` | Currently using                          |
| `next-docs`      | Someone took this name so I can't use it |

~~everything is gfm~~
